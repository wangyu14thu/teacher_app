# 📊 数据库集合清单

## 必须创建的数据库集合

请在**微信开发者工具 → 云开发 → 数据库**中创建以下集合：

### ✅ 已创建（用于学校申请流程）

1. **`schools_pending`** - 学校申请（待审核）
2. **`application_logs`** - 申请日志  
3. **`reviewers`** - 审核员表
4. **`projects_pending`** - 项目申请（待审核）

### 🆕 需要新创建（用于审核通过后）

5. **`schools`** - 正式学校表
   - **用途**: 存储审核通过的学校信息
   - **权限**: 所有用户可读，仅管理员可写

6. **`system_messages`** - 系统消息表
   - **用途**: 存储发送给用户的系统通知
   - **权限**: 仅创建者可读写

---

## 🔧 创建步骤

### 1. 打开云开发控制台
- 微信开发者工具顶部 → 点击**"云开发"**按钮

### 2. 进入数据库
- 左侧菜单 → 点击**"数据库"**

### 3. 创建集合
- 点击 **"+"** 按钮
- 输入集合名称（如 `schools`）
- 点击**"确定"**

### 4. 设置权限
- 点击集合名称
- 点击**"权限设置"**标签
- 选择合适的权限模式：
  - `schools`: **所有用户可读，仅创建者及管理员可写**
  - `system_messages`: **仅创建者可读写**

---

## 📝 集合说明

### `schools` - 正式学校表

审核通过后创建的学校记录，包含：
- 学校基本信息（名称、地址、规模）
- 管理员信息
- **邀请码** ⭐
- 成员数量
- 项目数量
- 下载额度

### `system_messages` - 系统消息表

发送给用户的各类系统通知：
- 学校审核结果（通过/驳回）
- 项目审核结果
- 邀请码通知
- 其他系统通知

消息包含：
- 标题、内容
- 已读/未读状态
- 创建时间
- 关联数据（如邀请码）

---

## ⚠️ 重要提示

**创建这两个集合后，才能完整测试学校审核功能！**

- ✅ 审核通过 → 生成邀请码 → 创建正式学校记录 → 发送系统消息
- ✅ 审核驳回 → 发送系统消息（包含驳回原因）

