# AI助手每日使用限制功能说明

## 功能概述

为了合理控制API调用成本，现在已为AI助手添加了每日使用次数限制功能：
- **每个用户每天最多可以提问30次**
- 次数按自然日（00:00-23:59）重置
- 超过限额后将无法继续提问，直到第二天

## 一、数据库准备

### 1. 创建 `ai_usage` 集合

在微信开发者工具的云开发控制台中：

1. 打开 **云开发控制台**
2. 进入 **数据库** 标签
3. 点击 **添加集合**
4. 集合名称：`ai_usage`

### 2. 设置权限

点击 `ai_usage` 集合的权限设置，选择：
- **所有用户可读，仅创建者可写**

或者使用自定义安全规则：
```json
{
  "read": "auth.openid == doc.userId",
  "write": "auth.openid == doc.userId"
}
```

### 3. 数据结构

`ai_usage` 集合的文档结构：

```javascript
{
  "_id": "自动生成",
  "userId": "用户的openid",
  "date": "2026-02-01",  // 日期格式：YYYY-MM-DD
  "count": 5,             // 今天已使用次数
  "lastUsedAt": Date,     // 最后使用时间
  "createdAt": Date       // 创建时间
}
```

### 4. 添加索引（可选，提升性能）

在 `ai_usage` 集合中添加复合索引：
1. 点击 **索引管理**
2. 添加索引：
   - 字段1: `userId` (升序)
   - 字段2: `date` (降序)
   - 索引名称: `userId_date`

## 二、部署云函数

### 1. 上传并部署 `ai-assistant` 云函数

确保云函数包含以下更新：
- `checkDailyUsage()` - 检查每日限额
- `recordUsage()` - 记录使用次数
- `getUsageStats()` - 获取使用统计

部署步骤：
1. 右键点击 `cloudfunctions/ai-assistant`
2. 选择 **上传并部署：云端安装依赖**
3. 等待部署完成

## 三、前端功能说明

### 1. 显示剩余次数

在AI助手页面顶部会显示：
- **正常状态**：`今日剩余：25/30次`（紫色渐变背景）
- **即将用完**：`今日剩余：3/30次 💡 提问次数不多了`（黄色提示）
- **已用完**：`今日剩余：0/30次 ⚠️ 今日次数已用完`（红色警告）

### 2. 限额拦截

当用户尝试超过限额提问时：
- 弹出提示框：`您今天的提问次数已用完（30次），明天再来吧！💡提示：合理规划问题可以更高效地使用AI助手。`
- 发送按钮自动禁用（剩余次数为0时）

### 3. 实时更新

每次成功发送消息后：
- 自动扣减1次剩余次数
- 前端UI实时更新显示

## 四、使用流程

```
用户输入问题
    ↓
点击发送
    ↓
检查是否超限
    ↓
是 → 显示提示，拒绝发送
    ↓
否 → 调用AI API
    ↓
记录使用次数（+1）
    ↓
更新前端显示（-1）
    ↓
返回AI回复
```

## 五、管理员调整限额

如需修改每日限额，在以下文件中修改 `DAILY_LIMIT` 常量：

**云函数端**：
```javascript
// cloudfunctions/ai-assistant/index.js
async function checkDailyUsage(openid) {
  const DAILY_LIMIT = 30; // 修改此处的数字
  // ...
}
```

**前端端**（用于显示）：
```javascript
// pages/ai-assistant/ai-assistant.js
data: {
  dailyLimit: 30  // 修改此处的数字（需与云函数保持一致）
}
```

## 六、测试建议

### 1. 正常使用测试
- 提问1-2次，检查剩余次数是否正确扣减
- 查看云数据库 `ai_usage` 是否有记录生成

### 2. 限额测试
- 在云函数中临时将 `DAILY_LIMIT` 改为 3
- 重新部署云函数
- 提问3次后，尝试第4次，应该被拦截

### 3. 跨日测试
- 查看第二天剩余次数是否重置为30

## 七、成本估算

按照当前配置（每日30次限额）：
- **单用户日成本**：约 0.06-0.12 元（假设每次消耗2000 tokens）
- **100用户日成本**：约 6-12 元
- **1000用户日成本**：约 60-120 元

建议根据实际预算调整每日限额。

## 八、常见问题

### Q1: 用户反馈次数扣减不准确？
检查云函数 `recordUsage()` 是否正确执行，查看云函数日志。

### Q2: 限额重置时间不对？
当前使用服务器时区，确保云函数部署在正确的地区。

### Q3: 想为VIP用户提供更高限额？
可以在 `checkDailyUsage()` 中根据用户等级动态调整 `DAILY_LIMIT`。

### Q4: 如何查看所有用户的使用情况？
在云开发控制台的 `ai_usage` 集合中可以查看所有记录。

---

**部署完成后，请测试以下场景**：
1. ✅ 首次使用，显示剩余30次
2. ✅ 提问后，剩余次数减少
3. ✅ 达到30次后，无法继续提问
4. ✅ 第二天重置为30次

如有问题，请检查云函数日志和数据库记录。

