<!--pages/tools/tools.wxml-->
<view class="tools-container">
  <view class="page-header">
    <text class="header-title">实操工具</text>
    <text class="header-subtitle">教学实用工具箱</text>
  </view>

  <view class="categories-list">
    <view class="category-section" wx:for="{{toolCategories}}" wx:key="id">
      <view class="category-title">
        <text class="title-icon">{{item.icon}}</text>
        <text class="title-text">{{item.name}}</text>
        <view class="title-line" style="background: linear-gradient(90deg, {{item.color}} 0%, transparent 100%);"></view>
      </view>
      
      <view class="tools-list">
        <view 
          class="tool-card" 
          wx:for="{{item.tools}}" 
          wx:for-item="tool" 
          wx:key="id"
          bindtap="viewTool"
          data-category-id="{{item.id}}"
          data-tool-id="{{tool.id}}"
        >
          <view class="tool-info">
            <text class="tool-title">{{tool.title}}</text>
            <text class="tool-subtitle">{{tool.subtitle}}</text>
          </view>
          <view class="tool-right">
            <view class="tool-points">
              <text class="points-icon">💎</text>
              <text class="points-text">{{tool.points}}</text>
            </view>
            <text class="tool-arrow">›</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>

