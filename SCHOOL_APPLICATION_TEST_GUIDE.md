# 学校申请到审核员接收通知 - 完整测试指南

## 🎯 功能说明

当教师端用户提交"创建学校团队"申请后，系统会：
1. ✅ 将申请数据保存到云数据库
2. ✅ 自动分配给空闲的审核员
3. ✅ 审核员在任务看板可以立即看到新任务

---

## 📋 前置准备

### 1. 初始化云数据库

#### 创建审核员账号（必需）

在云开发控制台 → 数据库 → `reviewers` 集合中添加：

```json
{
  "account": "reviewer",
  "password": "123456",
  "name": "审核员张三",
  "role": "reviewer",
  "permissions": ["review_projects", "review_schools"],
  "department": "内容审核部",
  "gradeLevel": ["1-2年级", "3-4年级", "5-6年级"],
  "status": "active",
  "loginFailures": 0,
  "currentToken": "",
  "lastLoginOpenid": "",
  "createdAt": "2026-01-31T00:00:00.000Z",
  "createdBy": "system"
}
```

**重要**：记录下创建后的 `_id`，这是审核员ID。

#### 创建必需的数据库集合

确保以下集合已创建（可以为空）：
- ✅ `reviewers` - 审核员账号
- ✅ `schools_pending` - 待审核学校（自动创建）
- ✅ `application_logs` - 申请日志（自动创建）

---

### 2. 部署云函数

#### 部署学校申请云函数

```bash
# 在微信开发者工具中
# 右键点击 cloudfunctions/school-application
# 选择"上传并部署：云端安装依赖"
```

#### 更新审核员云函数

```bash
# 右键点击 cloudfunctions/reviewer
# 选择"上传并部署：云端安装依赖"
```

**等待部署完成**（约1-2分钟）

---

## 🧪 测试步骤

### 第1步：教师端提交申请

1. 在微信开发者工具中，编译并预览教师端小程序
2. 进入 **我的 → 创建学校团队**
3. 填写学校信息：
   - 学校名称：翠微小学
   - 学校地址：北京市海淀区翠微路10号
   - 您的姓名：张主任
   - 联系电话：13800138000
   - 您的职务：教务主任
   - 学校规模：选择任意
   - 申请说明（可选）
4. 点击 **"下一步"**
5. 在确认页面点击 **"确认信息并提交审核"**
6. 确认提交

**预期结果**：
- ✅ 显示"申请已提交！"弹窗
- ✅ 提示将在24小时内审核
- ✅ 点击"我知道了"后返回上一页

---

### 第2步：查看云数据库

打开云开发控制台 → 数据库 → `schools_pending` 集合

**应该看到新增的一条记录**：

```json
{
  "_id": "xxx",
  "schoolName": "翠微小学",
  "schoolAddress": "北京市海淀区翠微路10号",
  "contactName": "张主任",
  "contactPhone": "13800138000",
  "position": "教务主任",
  "status": "pending",
  "assignedTo": "审核员的_id",  // 重点检查这个字段
  "submitTime": "2026-01-31T...",
  ...
}
```

**关键字段检查**：
- ✅ `status` 应为 `"pending"`
- ✅ `assignedTo` 应有值（审核员ID）
- ✅ `submitTime` 有正确的时间戳

---

### 第3步：审核员端查看任务

#### 3.1 登录审核员账号

1. 在微信开发者工具中，点击顶部"普通编译"下拉菜单
2. 选择"添加编译模式"
3. 启动页面选择：`pages/reviewer-login/reviewer-login`
4. 确定后编译
5. 输入账号：`reviewer`
6. 输入密码：`123456`
7. 点击"进入审核工作台"

#### 3.2 查看任务列表

登录成功后，应自动进入任务看板页面：

**预期显示**：
- ✅ 顶部统计卡片显示数据
- ✅ 切换到 **"待审核学校"** 选项卡
- ✅ 能看到刚才提交的学校申请
- ✅ 显示学校名称、联系人、地址
- ✅ 显示提交时间（如"刚刚"、"5分钟前"）

#### 3.3 点击查看详情

1. 点击学校申请卡片
2. 应该跳转到学校审核详情页
3. 能看到完整的学校信息

---

## 🔍 调试方法

### 如果看不到任务

#### 检查1：云函数日志

云开发控制台 → 云函数 → `school-application` → 日志

查找：
```
学校申请已创建: xxx, 分配给审核员: yyy
```

#### 检查2：审核员ID是否匹配

在 `schools_pending` 记录中的 `assignedTo` 字段，必须与登录的审核员的 `_id` 匹配。

**获取当前登录审核员ID**：
在审核员首页，打开控制台输入：
```javascript
wx.getStorageSync('reviewer_info')
```
查看 `id` 字段。

#### 检查3：Token是否有效

在审核员首页，控制台输入：
```javascript
wx.getStorageSync('reviewer_token')
```
应该返回类似：`reviewer_xxx_timestamp_random`

#### 检查4：云函数调用

在审核员首页 `index.js` 的 `loadPendingSchools` 函数中添加日志：
```javascript
console.log('审核员ID:', reviewerInfo.id);
console.log('调用结果:', result);
```

---

## 🐛 常见问题

### Q1: 提交后显示"提交失败"

**可能原因**：
- 云函数未部署或部署失败
- 网络问题

**解决方法**：
1. 检查云函数是否部署成功
2. 查看云函数日志是否有错误
3. 重新部署云函数

---

### Q2: 审核员看不到任务

**可能原因**：
- `assignedTo` 字段与审核员ID不匹配
- 审核员Token失效
- 数据库集合不存在

**解决方法**：
1. 检查数据库中的 `assignedTo` 值
2. 重新登录审核员账号
3. 手动修改数据库，将 `assignedTo` 改为正确的审核员ID

---

### Q3: 提交时报错"没有可用的审核员"

**可能原因**：
- `reviewers` 集合中没有 `status: "active"` 的审核员

**解决方法**：
确保审核员记录的 `status` 字段为 `"active"`

---

### Q4: 显示"Token无效"

**可能原因**：
- 审核员未登录
- Token过期

**解决方法**：
重新登录审核员账号

---

## 📊 数据流程图

```
【教师端】
  ↓ 点击"确认提交"
【school-team.js】doSubmit()
  ↓ 调用云函数
【school-application】submitApplication()
  ↓ 1. 检查重复申请
  ↓ 2. 自动分配审核员
  ↓ 3. 创建 schools_pending 记录
  ↓ 4. 记录日志
【云数据库】schools_pending (status: pending)
  ↓
【审核员端】
  ↓ 登录后自动加载
【reviewer/home/index.js】loadPendingSchools()
  ↓ 调用云函数
【reviewer】getPendingTasks()
  ↓ 查询 schools_pending
  ↓ WHERE: status='pending' AND assignedTo=审核员ID
【返回任务列表】
  ↓
【审核员看到任务】✅
```

---

## ✅ 验证清单

测试完成后，确认以下项目：

- [ ] 教师可以成功提交学校申请
- [ ] 提交后显示成功提示
- [ ] 云数据库 `schools_pending` 有新记录
- [ ] 新记录的 `assignedTo` 有值
- [ ] 审核员可以登录
- [ ] 审核员任务看板显示统计数据
- [ ] 审核员可以看到学校申请
- [ ] 点击学校申请可以查看详情
- [ ] 学校申请信息完整正确

---

## 🎉 测试成功标志

当您看到：
1. ✅ 教师提交后显示"申请已提交"
2. ✅ 数据库有新记录
3. ✅ 审核员首页显示"待审核学校"数量 > 0
4. ✅ 审核员可以点击查看详情

**恭喜！完整流程已打通！** 🎊

---

## 📝 下一步

- [ ] 测试审核通过流程（生成邀请码）
- [ ] 测试审核驳回流程
- [ ] 测试系统消息通知
- [ ] 测试项目申请流程

---

**文档版本**: v1.0  
**创建时间**: 2026-01-31  
**适用环境**: 微信小程序开发环境

